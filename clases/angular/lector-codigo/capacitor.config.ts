import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'co.edu.ue.lectorcodigo',
  appName: 'LectorCodigo',
  webDir: 'dist/lector-codigo/browser'
};

export default config;
